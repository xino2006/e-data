階層化された文書、情報を上下、左右で検証、まとめを自動で行えるようにする。
すべてテキストデータが前提(Wordは埋め込んだ情報をマクロで抽出できるようにする。ずっとあと）

TODO

・文法を作る
とりあえずC言語用
コメントの中に書く(//の後ろ　or　/*　　*/　の間　→　処理の都合上「//」のみに対応)
Cの文法は拾わないが考慮はする。(文章は先頭から始まるが、Cだと下位関数が先頭にあることがある。）
先頭から解析する。
1つのファイル1つの単位として扱う。
ディレクトリ階層がある。
.hファイルがある。
別ファイルに呼び出し関数がある。

一般的な事項：日時、バージョン、人名、関数名、概要、使い方、


